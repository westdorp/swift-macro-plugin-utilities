name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Package Gate
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Test
        run: make test
